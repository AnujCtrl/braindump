{
  "$schema": "../../../node_modules/@tauri-apps/cli/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for braindump desktop. Both windows can invoke commands defined in main.rs.",
  "windows": ["capture", "dashboard"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
